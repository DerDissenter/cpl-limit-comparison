{
  "name": "cpl-limit-comparison",
  "private": true,
  "scripts": {
    "check": "node --check main.js && node --check worker/worker.js",
    "test": "node --test",
    "worker:dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
